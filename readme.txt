1. If you have installed an older version of Fences, uninstall it, clean the registry and restart the computer.
2. Install program DO NOT restart (even if it is requested), copy the patch into the installation folder and start it as admin, 
now follow the instructions of the patch. Save "pointer.bat"!
3. Restart the computer after the patch is done, the restart can now take a little longer, especially if you have installed "over", as the final patching process is now in progress.
Block app in Firewall.